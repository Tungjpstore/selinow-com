# Project structure target

Use the existing project as the source of truth. Refactor incrementally toward the component tree in `04_COMPONENT_SYSTEM/ASTRO_COMPONENT_TREE.md`.

Do not rename routes, API endpoints, payload fields, or persisted states solely to match this kit. Add adapters/mapping at the UI boundary where necessary.
