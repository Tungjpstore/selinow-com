# Multi-Tenancy

Every business object belongs to a tenant and usually a store.
Authorization is enforced server-side.
IDs are non-sequential externally.
Exports, jobs, logs, and caches retain tenant scope.
Enterprise isolation can later use dedicated database or storage boundaries.
