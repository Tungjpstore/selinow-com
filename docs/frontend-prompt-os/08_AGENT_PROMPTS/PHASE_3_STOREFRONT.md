Read AGENTS.md. Activate `selinow-storefront` and `selinow-visual-qa`.

Implement storefront home, product detail, cart, checkout, order status, and key reveal. Prioritize 390px. Render catalog without JavaScript. Server reconfirms price/stock/payment/access. Do not treat return URL as paid.
