# Landing Page Pattern

Order: navigation → hero → proof → core workflow → feature clusters → use cases → trust → pricing preview → FAQ → final CTA.

Hero should answer: what it is, who it is for, what outcome it creates, and the next action.
Primary CTA: `Start building`. Secondary CTA: `View demo`.
