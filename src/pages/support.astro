---
import PlatformLayout from "../layouts/PlatformLayout.astro";
import MarketingHeader from "../components/marketing/MarketingHeader.astro";
import MarketingFooter from "../components/marketing/MarketingFooter.astro";
import PublicationGate from "../components/marketing/PublicationGate.astro";
import { getBindings } from "../lib/platform/bindings";
import { normalizeSupportedLocale } from "../lib/i18n/locale";

const env = getBindings();
const locale = normalizeSupportedLocale(Astro.locals.locale ?? env.DEFAULT_LOCALE);
Astro.response.headers.set("Cache-Control", "public, max-age=300");
---

<PlatformLayout
  title="Support status | Selinow"
  description="Publication status of Selinow support channels and operational assistance."
  locale={locale}
  canonical="https://selinow.com/support"
>
  <MarketingHeader dashboardOrigin={env.DASHBOARD_ORIGIN} locale={locale} />
  <PublicationGate
    eyebrow="SUPPORT INTAKE"
    title="Public support contact is not approved."
    detail="Blocked pending an owner-approved support identity, response scope, and abuse-handling process. This page does not collect messages."
    aside="Support channels will be enabled once operating procedures and response commitments are formally ratified."
    locale={locale}
  />
  <MarketingFooter dashboardOrigin={env.DASHBOARD_ORIGIN} locale={locale} />
</PlatformLayout>
